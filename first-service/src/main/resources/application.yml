server:
  port: 8080
  servlet:
    context-path: /organization-service

spring:
  datasource:
    url: jdbc:postgresql://dev-pg12:5432/soa
    username: soa
    password: soasoa
    driver-class-name: org.postgresql.Driver
  jpa:
    generate-ddl: true
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect

logging:
  level:
    org.fergoeqs: DEBUG
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE